class Solution {
    public String reverseWords(String s) {
        
        String strArr[] = s.trim().split("\\s+");
        String ans = "";
        for(int i = strArr.length-1;i>0;i--){
            ans = ans + strArr[i] +" ";
        }
        return ans+strArr[0];
    }
   
}
